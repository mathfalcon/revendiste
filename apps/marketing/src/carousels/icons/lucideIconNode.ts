/**
 * Tuple format produced by lucide-react icon modules (`__iconNode`).
 * Attribute values are strings in the shipped bundle.
 */
export type LucideCarouselIconNode = ReadonlyArray<
  readonly [tag: string, attrs: Readonly<Record<string, string>>]
>;
