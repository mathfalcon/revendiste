/**
 * Placeholder for a guided Playwright flow (publish listing).
 * Implement selectors against the real Revendiste frontend when stable.
 */
export const postListingFlowDescription =
  'Navigate authenticated user to listing creation and capture each step for carousel slides.';
